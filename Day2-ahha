<!DOCTYPE html>
<html lang="en">
<head>
    <link href="통계초ㅓㅇ.png" rel="shortcut icon" type="image/x-icon">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>통계청</title>
</head>

<body>
    <script>
         /*alert("404 not found");        
         document.write("5+6");
         console.log(5+6);
        var rank;
        //var score=87;
        let score=window.prompt("입력");

        if(score>=95){
            console.log("A+");
            rank = "A+"
        }else if(score>=90){
            console.log("A");
            rank = "A"
        }else if(score>=85){
            console.log("B+");
            rank = "B+"
        }else if(score>=80){
            console.log("B");
            rank = "B"
        }else if(score<=79){
            console.log("F");
            rank = "F"
        }

        switch(rank){
            case 'A+':
                console.log('platinum'); break;
            case 'A':
                console.log('gold'); break;
            case 'B+': 
                console.log('silver'); break;
            case 'B':
                console.log('Iron'); break;
            case 'F':
                console.log('UnRank'); break;
        }
        const colors= ['red', 'bule', 'green', 'yellow']
        for(let i= 0; i< 1; i++) {
            console.log(colors[i]);
        }*/
        /*const colors=['red','blue','green'];
        var i=0
        while (colors[i]!=null){
            console.log(colors[i]);
            i++;
        }
        var arr=[];
        arr[0]=zero;
        arr[1]=one;
        arr[2]=two;
        var arr=[,,,];//익숙해 지는게 좋을듯

        for(vari = 0; i<arr.length; i++) {
            console.log(arr[i]);
        }
        var arr= new Array('zero','one','two')*/

        /*function sum(a){
            return a;
        }
        console.log(sum(2));*/

        /*const sum = function(a){
            return a;
        }
        let a=window.prompt("입력");
        console log(sum(a));*/
        
       /*const hello = function() {
            return 'heillo';
        }
        console.log(hello());*/

        const rand1 = Math.random();
        const rand2 = Math.random();
        const rand3 = Math.random();

        document.write(rand1 + '<br>');
        document.write(rand2 + '<br>');
        document.write(rand3 + '<br>');

        const rand = function(min, max) {
            return Math.floor(Math.random() * (max-min +1))+min;
        }

        document.write(rand(1,3));
        document.write(rand(77,88));

    </script>
</body>
    <div id="result">
    </div>
    <script>
        document.getElementById("result").innerHTML=5+6;
    </script>
</html>
